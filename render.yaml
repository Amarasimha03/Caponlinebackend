services:
  - type: web
    name: online-exam-app
    env: static
    autoDeployTrigger: commit
    buildCommand: npm run build
    publishDir: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
